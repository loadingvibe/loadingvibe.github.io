# Logo 动画第五版 · 连续落笔与无残影交接

日期：2026-09-19。审核页 `/motion-review/`；欢迎页 `/?welcome=1`。仅本地修改，未提交、未发布。

## 最新追加：强调灵感的舒展，整段延长至 7.4 秒

- 单颗星光时长从 0.8 秒增加至 1.6 秒；开场尺寸从 0.86 改为 0.16，先出现小光点、再明显展开为完整星形。亮度先于尺寸建立，没有位置漂移、过冲或回弹。
- 全组星光 1.67–3.59 秒错峰完成，3.82 秒才开始笔触移动，留出约 0.23 秒完整展示。黑板仍无星光绘图。
- 后半段所有相关时序统一后移 1.8 秒：笔触、黑板淡出、跳跃、收腿、地影、原图交接和欢迎文字。审核页总时长、采样帧数、关键时刻与交接检查都同步更新。
- 对九个星光共 4005 个状态进行单调性/边界检查：透明度与尺寸单调，无位移、过冲；最大相邻尺寸变化 0.017498。检查了星光完成早于笔触启动、收腿早于隐腿、笔触归位早于原图交接的时序关系。
- 浏览器重新采样 445 帧：全部有限，笔触中心线越界 0，人物横移 0，动作后源网格归位误差 0，星光漂移 0；隐腿代理点遮挡仍为 100%。交接入口/出口平均像素差 0.012037 / 0.010489，最终保持差 0。
- 完整 1× 播放 883 帧，中位数 8.3ms，P95 9.2ms，>50ms 为 0；仅代表本机本次播放。构建 37 文件检查、23 路由与 2 sitemap 烟测、diff 空白检查通过，原有大包提示仍在。
- 放大检查微光/舒展/成形三态及后移的跳跃帧；人工检查新星光区间 F096–F239 的六组连续画面。证据：`star-unfold-early.png`、`star-unfold-middle.png`、`star-unfold-settled.png`、`star-unfold-sheet-5.png` 至 `star-unfold-sheet-10.png`。其余历史截图保持原样，不代表新时长。

## 同日追加：星光渐显与黑板简化

- 根据后续反馈，移除黑板绘图上的全部星光副本，仅保留六条笔触；人物右上方的灵感星光保留。
- 星光不再从 0.22 倍快速弹到 1.045 倍：改为透明度 0→1 用时 0.72 秒，大小 0.86→1 用时 0.80 秒；各元素错开 0.025 秒，中心始终固定，无回弹。关闭星光材质的 alpha 裁剪及深度写入，使微弱开场不会被阈值直接丢弃。
- 对 9 个元素各采样 337 帧（3033 个状态）：透明度与尺寸单调递增，无过冲、无位移；60fps 最大相邻透明度变化 0.043380、尺寸变化 0.005466。
- 整段重新检查 337 帧，有限坐标、中心线边界、人物横移、原图姿势归位、星光停稳检查通过。交接入口/出口平均像素差 0.012037 / 0.010489，最终保持差 0。
- 视觉核对黑板开场无星光绘图；放大比较微光初现、渐显中段、成形三态，逐帧浏览 F096–F167。新证据为 `star-fade-early.png`、`star-fade-middle.png`、`star-fade-settled.png` 和 `star-fade-sheet-5.png` 至 `star-fade-sheet-7.png`。下方 `v5-*` 截图与记录保留追加修改之前的历史状态。

## 本轮完成

1. 石墨笔画的最后一段从“按段显示”改成连续延长，末端位置和纹理随笔尖更新。
2. 笔杆绑定手腕，双关节运动驱动实际笔尖接触笔画；修正关节求解中的平面距离与角度截断。收笔后增加向上、向后抬起的动作，再淡出笔杆。
3. 原图交接改成两幅完整渲染结果的合成，去掉旧网格边缘残留。初测发现普通线性 8 位渲染目标存在暗部量化色差，改为支持时使用半浮点目标；画布和合成目标使用相同实际像素尺寸。
4. 审核页增加抬笔、原图交接停靠点、笔迹端点/握持检查、交接像素检查与慢帧时间记录。加载、全帧检查期间禁用操作，检查失败不冒充完成。
5. 保留上一版原图人物轮廓和纹理、独立笔触直接归位、星光右上冒出后固定、人物只纵向轻跳。

## 实测

- 桌面审核视口 1280×720，60fps 固定采样 337 帧：全部有限；笔触中心线越界 0；人物横移 0；动作结束后的源网格误差 0；星光停稳后位移 0。
- 实际笔尖对笔迹末端最大距离约 `5.66e-16` 世界单位，握持位置误差 0。最小手头代理间距 `0.262490`。这些不等同于整个角色表面无穿插的证明。
- 笔触中心线最大相邻采样帧位移 `0.0506731`；隐腿后两个采样帧的 24 个代理点几何遮挡率均为 100%。仍不是含透明纹理的完整像素遮挡检测。
- 人工浏览了 15 组连续缩略图，覆盖 F000–F336；另放大检查落笔、抬笔、交接中间态，以及实际欢迎页。没有看到整条笔触瞬跳、人物横向滑动或最终残留旧边缘。
- 修复后独立完整 1× 播放记录 666 帧，中位数 8.3ms，P95 8.4ms，超过 50ms 为 0。早期与构建同时进行的首轮测试曾有 1 个长帧；该结果不被抹去，也不能据后续一次测试保证所有设备无卡顿。
- 交接边界两侧相差 0.00002 秒的画布比较：入口平均差从 0.259 降到 **0.011861**，出口从 0.269 降到 **0.010489**（通道范围 0–255；采用预乘 RGB/alpha 的每像素最大差，再取全画布平均）。入口仍有 2 个边缘像素差超过 2，最大 5；不是逐像素绝对相等。最终 5.38 与 5.60 秒完全相同。
- 在 390×844 响应式视口再次运行 337 帧检查：有限坐标、边界、人物横移、归位误差、星光漂移检查均通过；交接入口/出口平均差 0.023018 / 0.013105，最终保持差 0。该测试是桌面浏览器响应式模拟，不是真机性能测试。
- 桌面与窄屏实际欢迎页均到达 `static` / `complete`；390×844 页面无横向溢出，文字、按钮完整。点击进入书架后展示导航和三本书。视口检查后已恢复默认尺寸。
- 审核页控制台未记录 warning/error。生产构建：37 个文件，Astro 0 errors / 0 warnings / 0 hints；静态烟测 23 路由、2 sitemap 通过；`git diff --check` 通过。Vite 仍有 Three.js 相关 chunk 大于 500KB 的提示，未隐藏。

## 证据与边界

连续帧：`v5-sheet-01.png` 至 `v5-sheet-15.png`。关键画面：`v5-handoff.png`、`v5-lift-mobile.png`、`v5-welcome-mobile.png`。审核页“检查全部 337 帧”后可下载带逐帧数值的第五版 JSON。

人物仍是为固定正面镜头构建的三维浅浮雕，不是任意角度的全身角色资产。腿部为新增结构，其审美是否满意仍需用户确认。本轮解决连续性与交接问题，不宣称建模已达到影视级或与原图完全相同。若仍不接受腿部造型，建议取消腿和跳跃、保留原图上半身故事；本轮未擅自启用这个削减版本。
